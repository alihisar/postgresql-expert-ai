# Gold Answer

A strong answer:

- classifies shared and private memory correctly
- explains concurrency multiplication
- treats `effective_cache_size` as a planner estimate rather than allocation
- includes OS memory
- recommends query-local experiments first
- validates with temp I/O, latency, concurrency, and system memory pressure
